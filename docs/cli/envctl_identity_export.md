## envctl identity export

Export identity for backup

### Synopsis

Export your identity as a paper backup using recovery words.

The recovery words can be used to restore your identity on a new machine.
Store them securely - anyone with these words can become you.

Examples:
  envctl identity export --paper
  envctl identity export --paper --plain
  envctl identity export --paper --qr
  envctl identity export --paper --output backup.txt

```
envctl identity export [flags]
```

### Options

```
  -h, --help            help for export
      --output string   write to file instead of stdout
      --paper           export as paper backup (mnemonic words)
      --plain           plain text output (no box)
      --qr              display as QR code
```

### Options inherited from parent commands

```
      --config string   config file (default $HOME/.config/envctl/config.toml)
      --lang string     language for messages (default: auto-detect)
  -v, --verbose         verbose output
```

### SEE ALSO

* [envctl identity](envctl_identity.md)	 - Manage your identity

###### Auto generated by spf13/cobra on 6-Jan-2026

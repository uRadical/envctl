## envctl project create

Create a new project

### Synopsis

Create a new project with you as the founding admin.

The project uses a blockchain to track membership changes. As the founder,
you become the first admin and can invite other members.

By default, projects have three environments: dev, stage, prod.
You can customise this with the --envs flag.

Examples:
  envctl project create myproject
  envctl project create myproject --envs dev,qa,prod
  envctl project create myproject --envs local,test,live --default-access local

```
envctl project create <name> [flags]
```

### Options

```
      --default-access string   default environment access for new members (default "dev")
      --envs strings            environments for this project (comma-separated) (default [dev,stage,prod])
  -h, --help                    help for create
```

### Options inherited from parent commands

```
      --config string    config file (default $HOME/.config/envctl/config.toml)
      --lang string      language for messages (default: auto-detect)
  -p, --project string   project name (overrides .envctl file)
  -v, --verbose          verbose output
```

### SEE ALSO

* [envctl project](envctl_project.md)	 - Project management commands

###### Auto generated by spf13/cobra on 6-Jan-2026

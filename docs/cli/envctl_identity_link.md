## envctl identity link

Link identity to another device

### Synopsis

Transfer your identity to a new device securely.

On your existing device:
  envctl identity link

On your new device:
  envctl identity link --code XXXXXX

The devices will establish an encrypted connection using the code
for authentication. Your identity is transferred directly between
devices without passing through any server.

The pairing code is valid for 5 minutes and can only be used once.

```
envctl identity link [flags]
```

### Options

```
      --addr string   direct address of source device (optional)
      --code string   linking code from source device
  -h, --help          help for link
```

### Options inherited from parent commands

```
      --config string   config file (default $HOME/.config/envctl/config.toml)
      --lang string     language for messages (default: auto-detect)
  -v, --verbose         verbose output
```

### SEE ALSO

* [envctl identity](envctl_identity.md)	 - Manage your identity

###### Auto generated by spf13/cobra on 29-Dec-2025

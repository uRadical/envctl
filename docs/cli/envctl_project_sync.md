## envctl project sync

Sync the project chain and secrets with peers

### Synopsis

Synchronize the project chain and secrets with connected peers.

This command will:
1. Request the latest chain blocks from peers
2. Optionally request secrets for environments you have access to

Use this when:
- You've been offline and want to catch up
- You suspect your chain might be out of sync
- You want to ensure you have the latest secrets

Examples:
  envctl project sync
  envctl project sync --secrets

```
envctl project sync [flags]
```

### Options

```
  -h, --help      help for sync
      --secrets   also request secrets from peers
```

### Options inherited from parent commands

```
      --config string    config file (default $HOME/.config/envctl/config.toml)
      --lang string      language for messages (default: auto-detect)
  -p, --project string   project name (overrides .envctl file)
  -v, --verbose          verbose output
```

### SEE ALSO

* [envctl project](envctl_project.md)	 - Project management commands

###### Auto generated by spf13/cobra on 6-Jan-2026

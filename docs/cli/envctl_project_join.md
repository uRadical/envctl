## envctl project join

Join an existing project using an invite code

### Synopsis

Join an existing project that you've been invited to.

You need an invite code from a project admin. The admin creates the invite
using your public key (from 'envctl whoami --verbose').

This command will:
1. Connect to a peer who has the project chain
2. Download and verify the chain
3. Verify your invite code is valid
4. Create a member-add block (signed by you)
5. Broadcast the block to peers for inclusion

The project directory (.envctl/) will be created in the current directory.

Examples:
  envctl project join --code ABC-DEF-GHI

```
envctl project join [flags]
```

### Options

```
      --code string   invite code (e.g., ABC-DEF-GHI)
  -h, --help          help for join
```

### Options inherited from parent commands

```
      --config string    config file (default $HOME/.config/envctl/config.toml)
      --lang string      language for messages (default: auto-detect)
  -p, --project string   project name (overrides .envctl file)
  -v, --verbose          verbose output
```

### SEE ALSO

* [envctl project](envctl_project.md)	 - Project management commands

###### Auto generated by spf13/cobra on 6-Jan-2026

## envctl log

View audit logs

### Synopsis

View and filter audit logs.

In interactive mode, use arrow keys to navigate, / to search,
f to filter, and q to quit.

Examples:
  envctl log
  envctl log --level error
  envctl log --since 1h
  envctl log --follow
  envctl log --format json

```
envctl log [flags]
```

### Options

```
      --category string   filter by category (identity, project, secrets, etc.)
      --follow            follow new logs (like tail -f)
      --format string     output format (tui, table, json) (default "tui")
  -h, --help              help for log
      --level string      filter by level (debug, info, warn, error)
      --limit int         maximum entries to show (default 1000)
      --project string    filter by project
      --search string     search text
      --since string      show logs since (e.g., 5m, 1h, 24h, 2025-01-15) (default "24h")
      --until string      show logs until
```

### Options inherited from parent commands

```
      --config string   config file (default $HOME/.config/envctl/config.toml)
      --lang string     language for messages (default: auto-detect)
  -v, --verbose         verbose output
```

### SEE ALSO

* [envctl](envctl.md)	 - Zero-infrastructure secrets management for dev projects

###### Auto generated by spf13/cobra on 6-Jan-2026

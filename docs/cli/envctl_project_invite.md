## envctl project invite

Create an invite for a new member

### Synopsis

Generate a single-use invite code for a new team member.

The invitee must provide their public key beforehand (via envctl whoami --verbose).
Share the generated code with them to complete the join process.

Examples:
  envctl project invite alice --pubkey abc123...
  envctl project invite bob --pubkey abc123... --ttl 1h
  envctl project invite carol --pubkey abc123... --env dev,stage --role member

```
envctl project invite <display-name> [flags]
```

### Options

```
      --env string      environments to grant access to (comma-separated)
  -h, --help            help for invite
      --pubkey string   invitee's signing public key (hex)
      --role string     role: admin, member, or reader (default "member")
      --ttl string      invite expiration (e.g., 10m, 1h, 24h) (default "10m")
```

### Options inherited from parent commands

```
      --config string    config file (default $HOME/.config/envctl/config.toml)
      --lang string      language for messages (default: auto-detect)
      --project string   project name (overrides .envctl file)
  -v, --verbose          verbose output
```

### SEE ALSO

* [envctl project](envctl_project.md)	 - Project management commands

###### Auto generated by spf13/cobra on 29-Dec-2025

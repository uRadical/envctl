## envctl identity recover

Recover identity from paper backup

### Synopsis

Recover your identity using the 24 recovery words from a paper backup.

You will be prompted to enter the words and set a new passphrase.

Examples:
  envctl identity recover
  envctl identity recover --words "apple banana cherry ..."
  envctl identity recover --name myname --words "..."

```
envctl identity recover [flags]
```

### Options

```
  -h, --help           help for recover
      --name string    identity name (required)
      --words string   recovery words (space-separated)
```

### Options inherited from parent commands

```
      --config string   config file (default $HOME/.config/envctl/config.toml)
      --lang string     language for messages (default: auto-detect)
  -v, --verbose         verbose output
```

### SEE ALSO

* [envctl identity](envctl_identity.md)	 - Manage your identity

###### Auto generated by spf13/cobra on 6-Jan-2026

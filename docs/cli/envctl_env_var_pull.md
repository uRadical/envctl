## envctl env var pull

Pull operations from peers

### Synopsis

Pull operations from connected team members.

Requests the latest operations from peers who are members of this project.
Any new operations will be merged into your local chain.

The daemon must be running to communicate with peers.

Examples:
  envctl env var pull
  envctl env var pull -e prod

```
envctl env var pull [flags]
```

### Options

```
  -e, --env string   target environment (default: current environment)
  -h, --help         help for pull
```

### Options inherited from parent commands

```
      --config string   config file (default $HOME/.config/envctl/config.toml)
      --lang string     language for messages (default: auto-detect)
  -v, --verbose         verbose output
```

### SEE ALSO

* [envctl env var](envctl_env_var.md)	 - Manage environment variables via operations chain

###### Auto generated by spf13/cobra on 6-Jan-2026

## envctl identity pubkey

Output public key for invites

### Synopsis

Output your signing public key in hex format, ready to share for project invites.

This is the key that others need when running:
  envctl project invite <name> <pubkey>

Examples:
  envctl identity pubkey
  envctl identity pubkey | pbcopy   # Copy to clipboard on macOS

```
envctl identity pubkey [flags]
```

### Options

```
  -h, --help   help for pubkey
```

### Options inherited from parent commands

```
      --config string   config file (default $HOME/.config/envctl/config.toml)
      --lang string     language for messages (default: auto-detect)
  -v, --verbose         verbose output
```

### SEE ALSO

* [envctl identity](envctl_identity.md)	 - Manage your identity

###### Auto generated by spf13/cobra on 6-Jan-2026

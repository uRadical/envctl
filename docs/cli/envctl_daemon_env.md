## envctl daemon env

Print shell environment for daemon integration

### Synopsis

Print environment variables for shell integration.

This command outputs shell commands to set up environment variables
for integrating with the daemon. Add it to your shell profile:

  # bash/zsh
  eval "$(envctl daemon env)"

  # fish
  envctl daemon env | source

The following variables are set:
  ENVCTL_SOCKET  - Path to the daemon socket
  ENVCTL_RUNNING - Set to 1 if daemon is running

```
envctl daemon env [flags]
```

### Options

```
  -h, --help   help for env
```

### Options inherited from parent commands

```
      --config string   config file (default $HOME/.config/envctl/config.toml)
      --lang string     language for messages (default: auto-detect)
  -v, --verbose         verbose output
```

### SEE ALSO

* [envctl daemon](envctl_daemon.md)	 - Daemon management commands

###### Auto generated by spf13/cobra on 29-Dec-2025

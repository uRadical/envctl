## envctl project relay set

Set relay URL for this project (requires admin consensus)

### Synopsis

Set the relay server URL for this project.

This creates a proposal to update the project policy with the relay URL.
The proposal must be approved by enough admins according to project policy.

Use --disable to disable relay for this project.

Examples:
  envctl project relay set wss://relay.envctl.dev/ws
  envctl project relay set --disable

```
envctl project relay set [url] [flags]
```

### Options

```
      --disable   disable relay for this project
  -h, --help      help for set
```

### Options inherited from parent commands

```
      --config string    config file (default $HOME/.config/envctl/config.toml)
      --lang string      language for messages (default: auto-detect)
  -p, --project string   project name (overrides .envctl file)
  -v, --verbose          verbose output
```

### SEE ALSO

* [envctl project relay](envctl_project_relay.md)	 - Manage project relay settings

###### Auto generated by spf13/cobra on 6-Jan-2026

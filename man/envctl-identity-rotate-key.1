.nh
.TH "ENVCTL" "1" "Jan 2026" "uRadical" "envctl manual"

.SH NAME
.PP
envctl-identity-rotate-key - Rotate your identity keys


.SH SYNOPSIS
.PP
\fBenvctl identity rotate-key [flags]\fP


.SH DESCRIPTION
.PP
Generate a new key pair and re-encrypt all your secrets.

.PP
This operation:
1. Generates a new Ed25519 + ML-KEM key pair
2. Decrypts all secrets with your current key
3. Re-encrypts them with the new key
4. Atomically swaps the keys
5. Announces your new public key to team members

.PP
Your old key is backed up and securely deleted after 7 days.

.PP
Examples:
  envctl identity rotate-key
  envctl identity rotate-key --same-passphrase
  envctl identity rotate-key --local-only


.SH OPTIONS
.PP
\fB--force\fP[=false]
	skip confirmation prompt

.PP
\fB-h\fP, \fB--help\fP[=false]
	help for rotate-key

.PP
\fB--local-only\fP[=false]
	skip team announcement

.PP
\fB--same-passphrase\fP[=false]
	keep the same passphrase

.PP
\fB--search-dirs\fP=[]
	directories to search for secrets (default: current directory)


.SH OPTIONS INHERITED FROM PARENT COMMANDS
.PP
\fB--config\fP=""
	config file (default $HOME/.config/envctl/config.toml)

.PP
\fB--lang\fP=""
	language for messages (default: auto-detect)

.PP
\fB-v\fP, \fB--verbose\fP[=false]
	verbose output


.SH SEE ALSO
.PP
\fBenvctl-identity(1)\fP


.SH HISTORY
.PP
6-Jan-2026 Auto generated by spf13/cobra
